#include <stdio.h>
#include <string.h>
int main()
{
	int l, i, t, r;
	char input[31];
	int s[30] = { 0, };

	scanf("%s", input);
	l = strlen(input);

	

	t = -1;
	for(i = 0 ; i < l ; ++i)
	{
	}

	return 0;
}
