#include <stdio.h>
#include <stdbool.h>
typedef struct _Data Data;
struct _Data
{
	int x;
	int y;
	int c;
};
int main()
{
	bool v[1001][1001] = { 0, };
	int x, y, w, h;
	int f, r;
	Data q[1001];
	scanf("%d %d %d %d", &x, &y, &w, &h);
	while(
	return 0;
}
